import React from 'react'
import styles from './styles.css'

export default ({ currentPage = '' }) => (
    
<div id="join" className={styles.join}>
    <div className={styles.joinContainer}>
        <div><a className={styles.joinLink} href="#">Join us now</a></div>
    </div>
</div> 

)
